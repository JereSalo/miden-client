name: Publish Web Client SDK on Release

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  check-version:
    runs-on: ubuntu-24.04
    outputs:
      should_publish_web: ${{ steps.check_web_version.outputs.should_publish }}
      web_version: ${{ steps.check_web_version.outputs.current_version }}
      should_publish_react: ${{ steps.check_react_version.outputs.should_publish }}
      react_version: ${{ steps.check_react_version.outputs.current_version }}
      should_publish_vite: ${{ steps.check_vite_plugin_version.outputs.should_publish }}
      vite_version: ${{ steps.check_vite_plugin_version.outputs.current_version }}
    steps:
      - name: Check out code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: refs/tags/${{ github.event.release.tag_name }}

      - name: Ensure release tag has web-client version bump
        id: check_web_version
        run: ./scripts/check-web-client-version-release.sh "$GITHUB_SHA"

      - name: Ensure release tag has react-sdk version bump
        id: check_react_version
        run: ./scripts/check-react-sdk-version-release.sh "$GITHUB_SHA"

      - name: Ensure release tag has vite-plugin version bump
        id: check_vite_plugin_version
        run: ./scripts/check-vite-plugin-version-release.sh "$GITHUB_SHA"

  # Build native Node.js binaries for each platform
  build-native-nodejs:
    needs: check-version
    if: needs.check-version.outputs.should_publish_web == 'true'
    strategy:
      matrix:
        include:
          - os: macos-26
            platform-package: node-sdk-darwin-arm64
          - os: macos-26-intel
            platform-package: node-sdk-darwin-x64
          - os: ubuntu-24.04
            platform-package: node-sdk-linux-x64-gnu
    runs-on: ${{ matrix.os }}
    steps:
      - name: Check out code
        uses: actions/checkout@v6
        with:
          ref: refs/tags/${{ github.event.release.tag_name }}

      - name: Install Rust
        run: rustup update --no-self-update

      - name: Build napi binary
        run: cargo build -p miden-client-web --no-default-features --features nodejs --release

      - name: Prepare .node binary
        run: |
          if [ "${{ runner.os }}" = "macOS" ]; then
            EXT="dylib"
          else
            EXT="so"
          fi
          BINARY=$(find target -name "libmiden_client_web.${EXT}" -path "*/release/*" | head -1)
          if [ -z "$BINARY" ]; then
            echo "Binary not found"
            exit 1
          fi
          cp "$BINARY" "packages/${{ matrix.platform-package }}/miden_client_web.node"

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform-package }}
          path: packages/${{ matrix.platform-package }}/miden_client_web.node

  # Publish all packages to npm
  publish:
    needs: [check-version, build-native-nodejs]
    if: >
      !cancelled() &&
      needs.check-version.result == 'success'
    runs-on: ubuntu-24.04
    steps:
      - name: Check out code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: refs/tags/${{ github.event.release.tag_name }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org/'

      - name: Install Rust and wasm target
        run: |
          rustup update --no-self-update
          rustup target add wasm32-unknown-unknown

      - name: Install binaryen (wasm-opt)
        run: sudo apt-get update && sudo apt-get install -y binaryen

      # ── Publish Node.js platform packages ──
      - name: Download native binary artifacts
        if: needs.check-version.outputs.should_publish_web == 'true' && needs.build-native-nodejs.result == 'success'
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Sync platform package versions and pack (fork dry-run)
        if: needs.check-version.outputs.should_publish_web == 'true' && needs.build-native-nodejs.result == 'success'
        run: |
          WEB_VERSION=$(jq -r '.version' crates/web-client/package.json)
          for pkg in node-sdk-darwin-arm64 node-sdk-darwin-x64 node-sdk-linux-x64-gnu; do
            cp "artifacts/${pkg}/miden_client_web.node" "packages/${pkg}/miden_client_web.node"
            # Sync version with web-client
            jq --arg v "$WEB_VERSION" '.version = $v' "packages/${pkg}/package.json" > /tmp/pkg.json
            mv /tmp/pkg.json "packages/${pkg}/package.json"
            echo "Packing @miden-sdk/${pkg}@${WEB_VERSION}..."
            cd "packages/${pkg}"
            npm pack
            cd ../..
          done

      # ── Build and publish web-client ──
      - name: Install & build web-client
        if: needs.check-version.outputs.should_publish_web == 'true' || needs.check-version.outputs.should_publish_react == 'true'
        run: |
          cd crates/web-client
          yarn install --frozen-lockfile
          yarn build

      - name: Verify WASM is optimized
        if: needs.check-version.outputs.should_publish_web == 'true'
        run: |
          wasm_file=$(find crates/web-client/dist/assets -name '*.wasm' | head -1)
          size=$(stat --format=%s "$wasm_file")
          max_size=$((25 * 1024 * 1024))
          echo "WASM size: $((size / 1024 / 1024))MB ($size bytes)"
          if [ "$size" -gt "$max_size" ]; then
            echo "::error::WASM is $((size / 1024 / 1024))MB — exceeds 25MB limit. wasm-opt likely failed silently."
            exit 1
          fi

      - name: Inject Node.js optionalDependencies before publish
        if: needs.check-version.outputs.should_publish_web == 'true' && needs.build-native-nodejs.result == 'success'
        run: |
          cd crates/web-client
          node -e "
            const pkg = JSON.parse(require('fs').readFileSync('package.json', 'utf8'));
            const v = pkg.version;
            pkg.optionalDependencies = {
              '@miden-sdk/node-darwin-arm64': v,
              '@miden-sdk/node-darwin-x64': v,
              '@miden-sdk/node-linux-x64-gnu': v,
            };
            require('fs').writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
            console.log('Injected optionalDependencies with version ' + v);
          "

      - name: Pack web-client (fork dry-run)
        if: needs.check-version.outputs.should_publish_web == 'true'
        run: |
          cd crates/web-client
          npm pack

      # ── Build and publish react-sdk ──
      - name: Install & build react-sdk
        if: needs.check-version.outputs.should_publish_react == 'true'
        run: |
          cd packages/react-sdk
          yarn install
          yarn build

      - name: Pack react-sdk (fork dry-run)
        if: needs.check-version.outputs.should_publish_react == 'true'
        run: |
          cd packages/react-sdk
          npm pack

      # ── Build and publish vite-plugin ──
      - name: Install & build vite-plugin
        if: needs.check-version.outputs.should_publish_vite == 'true'
        run: |
          cd packages/vite-plugin
          yarn install
          yarn build

      - name: Pack vite-plugin (fork dry-run)
        if: needs.check-version.outputs.should_publish_vite == 'true'
        run: |
          cd packages/vite-plugin
          npm pack

      # ── Upload all packed tarballs for inspection ──
      - name: Upload packed tarballs
        uses: actions/upload-artifact@v4
        with:
          name: fork-dry-run-tarballs
          path: |
            packages/node-sdk-*/miden-sdk-*.tgz
            crates/web-client/miden-sdk-*.tgz
            packages/react-sdk/miden-sdk-*.tgz
            packages/vite-plugin/miden-sdk-*.tgz
          if-no-files-found: warn

      # ── Status ──
      - name: Done (web-client)
        if: needs.check-version.outputs.should_publish_web == 'true'
        env:
          CURRENT_VERSION: ${{ needs.check-version.outputs.web_version }}
        run: echo "✅ Web-client published version $CURRENT_VERSION with Node.js platform binaries."

      - name: Done (react-sdk)
        if: needs.check-version.outputs.should_publish_react == 'true'
        env:
          CURRENT_VERSION: ${{ needs.check-version.outputs.react_version }}
        run: echo "✅ React SDK published version $CURRENT_VERSION."

      - name: Done (vite-plugin)
        if: needs.check-version.outputs.should_publish_vite == 'true'
        env:
          CURRENT_VERSION: ${{ needs.check-version.outputs.vite_version }}
        run: echo "✅ Vite plugin published version $CURRENT_VERSION."

      - name: Skipped publish (web-client)
        if: needs.check-version.outputs.should_publish_web != 'true'
        env:
          SHOULD_PUBLISH: ${{ needs.check-version.outputs.should_publish_web }}
          CURRENT_VERSION: ${{ needs.check-version.outputs.web_version }}
        run: |
          echo "ℹ️ Web-client publish skipped; should_publish=$SHOULD_PUBLISH"
          if [ -n "$CURRENT_VERSION" ]; then
            echo "Current version: $CURRENT_VERSION"
          fi

      - name: Skipped publish (react-sdk)
        if: needs.check-version.outputs.should_publish_react != 'true'
        env:
          SHOULD_PUBLISH: ${{ needs.check-version.outputs.should_publish_react }}
          CURRENT_VERSION: ${{ needs.check-version.outputs.react_version }}
        run: |
          echo "ℹ️ React SDK publish skipped; should_publish=$SHOULD_PUBLISH"
          if [ -n "$CURRENT_VERSION" ]; then
            echo "Current version: $CURRENT_VERSION"
          fi

      - name: Skipped publish (vite-plugin)
        if: needs.check-version.outputs.should_publish_vite != 'true'
        env:
          SHOULD_PUBLISH: ${{ needs.check-version.outputs.should_publish_vite }}
          CURRENT_VERSION: ${{ needs.check-version.outputs.vite_version }}
        run: |
          echo "ℹ️ Vite plugin publish skipped; should_publish=$SHOULD_PUBLISH"
          if [ -n "$CURRENT_VERSION" ]; then
            echo "Current version: $CURRENT_VERSION"
          fi
